# allox-session-network

Optional persistent Session network namespaces and trusted HTTP/TCP forwarding for Allox OS.
Installing the wheel only registers the feature. `--session-network isolated` or `proxy`
activates it.
