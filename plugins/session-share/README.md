# allox-session-share

Optional Session-to-Session file forwarding for Allox OS. Installing the wheel only registers
the feature. `--share-tools` activates its RPC routes, Unix endpoints and Bubblewrap mounts.
