# Custom route: GET /allox-health → in-VM health server (port 9090)
location /allox-health {
    proxy_pass http://127.0.0.1:9090/health;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}
